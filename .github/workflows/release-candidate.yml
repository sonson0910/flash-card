name: Build release candidate

on:
  workflow_dispatch:
    inputs:
      revision:
        description: Full immutable revision to build and seal
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-candidate
  cancel-in-progress: false

jobs:
  validate_revision:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate immutable release revision
        shell: bash
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REVISION: ${{ inputs.revision }}
        run: |
          [[ "$REVISION" =~ ^([0-9a-f]{40}|[0-9a-f]{64})$ ]]
          test -n "$DEFAULT_BRANCH"
          test "$GITHUB_REF" = "refs/heads/$DEFAULT_BRANCH"
          test "$GITHUB_EVENT_NAME" = "workflow_dispatch"
          test "$GITHUB_SHA" = "$REVISION"

      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.revision }}
          fetch-depth: 0

      - name: Verify release revision ancestry and checkout
        shell: bash
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REVISION: ${{ inputs.revision }}
        run: |
          git fetch --no-tags origin "refs/heads/$DEFAULT_BRANCH:refs/remotes/origin/$DEFAULT_BRANCH"
          git merge-base --is-ancestor "$REVISION" "origin/$DEFAULT_BRANCH"
          test "$(git rev-parse HEAD)" = "$REVISION"
          test "$GITHUB_SHA" = "$REVISION"

  build:
    needs: validate_revision
    runs-on: ubuntu-latest
    timeout-minutes: 40
    environment: production
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.revision }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify exact release checkout
        shell: bash
        env:
          REVISION: ${{ inputs.revision }}
        run: |
          test "$(git rev-parse HEAD)" = "$REVISION"
          test "$GITHUB_SHA" = "$REVISION"

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm

      - uses: actions/setup-java@d7793b545071e98d581d3bf084a51c3213318a07 # v4
        with:
          distribution: temurin
          java-version: 21

      - run: npm ci
      - run: npm ci --prefix functions
      - run: npx playwright install --with-deps chromium firefox webkit

      - name: Verify production release configuration
        run: npm run verify:release-config
        env:
          RELEASE_REVISION: ${{ inputs.revision }}
          VITE_FIREBASE_APP_CHECK_SITE_KEY: ${{ secrets.VITE_FIREBASE_APP_CHECK_SITE_KEY }}

      - name: Build and verify the exact release candidate
        run: npm run verify
        env:
          CI: "true"
          RELEASE_REVISION: ${{ inputs.revision }}
          VITE_FIREBASE_APP_CHECK_SITE_KEY: ${{ secrets.VITE_FIREBASE_APP_CHECK_SITE_KEY }}

      - name: Seal verified deployable files
        run: >-
          node scripts/release-artifact.mjs seal
          --revision "${{ inputs.revision }}"
          --workflow-run-id "${{ github.run_id }}"
          --output artifacts/release-candidate-manifest.json

      - name: Publish approved digest in the workflow summary
        shell: bash
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          const manifest = JSON.parse(fs.readFileSync('artifacts/release-candidate-manifest.json', 'utf8'));
          fs.appendFileSync(
            process.env.GITHUB_STEP_SUMMARY,
            `Revision: \`${manifest.revision}\`\n\nCandidate SHA-256: \`${manifest.candidateSha256}\`\n`,
          );
          NODE

      - name: Retain browser failure evidence
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: browser-evidence-${{ inputs.revision }}
          path: |
            playwright-report
            test-results
          if-no-files-found: warn
          retention-days: 14

      - name: Upload the sealed release candidate
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: lingoflash-${{ inputs.revision }}
          path: |
            dist
            functions/lib
            functions/package.json
            functions/package-lock.json
            package.json
            package-lock.json
            firestore.rules
            firestore.indexes.json
            firebase.json
            firebase-applet-config.json
            artifacts/phase6-readiness.json
            artifacts/release-candidate-manifest.json
          if-no-files-found: error
          retention-days: 14
